# dependencies
node_modules/

# build output
dist/

# secrets & environment — never commit real values.
# .env.example IS committed (it is a template with no secrets).
.env
.env.*
!.env.example
*.sql
tests/

# generated JWT secret + any local SQLite leftovers from the pre-Supabase era
data/.jwt-secret
data/hcp.db*

# logs & OS / editor cruft
*.log
npm-debug.log*
yarn-error.log*
.DS_Store
Thumbs.db
.vscode/
.idea/
